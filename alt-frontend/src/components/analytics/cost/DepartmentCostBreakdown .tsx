import type { Tool } from "../../../types/tool";

export const DepartmentCostBreakdown = ({ tools }: { tools: Tool[] }) => {
  console.log("tools", tools);

  return <div>DepartmentCostBreakdown</div>;
};
